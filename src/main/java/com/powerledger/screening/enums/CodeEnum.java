package com.powerledger.screening.enums;

/**
 * Enum used as error code
 */
public enum CodeEnum {
    REQUIRED,
    INVALID_REQUEST,
    RESOURCE_NOT_FOUND,
    NOT_ALLOWED,
    UNKNOWN,
    INVALID_DATA
}
